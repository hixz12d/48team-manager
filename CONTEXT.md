# 身份与真相来源

这是自用 ChatGPT Team / Workspace 运营控制台。三个轴永远分开：

- 官方计划：`free` / `plus` / `pro` / `team` / `business` / `unknown`
- Workspace Role：`owner` / `admin` / `member` / `unknown`
- 本地用途：`mother` / `child` / `standby` / `free` / `disabled`

Gmail、iCloud、姓名、Team 前缀都不是身份真理。最多当 migration hint。

真相来源：

- 本地用途：local DB
- 官方 Workspace / role / 5h / 7d quota：OpenAI official APIs
- Sub2API：verified binding + runtime
- HME / Phone / Proxy：local lease 与健康检查
