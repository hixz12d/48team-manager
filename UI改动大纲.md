# 48 Team Manager UI 改动大纲

> 评审方式：impeccable（init → critique：独立设计评审 A + 机械检测器 B），隔离预览 `tests/preview_app.py`（8019，示例数据）截图 1440 / 1024 / 390 三档、深浅两套主题，外加源码走查。
> 产品上下文：见根目录 `PRODUCT.md`（本次新建）。
> 改动尺度（已确认）：**保留现有视觉风格，重做信息架构与细节**。不换配色、不引入框架或 UI 库，不加构建链。
> 使用前提（已确认）：只在电脑上用；10–40 个团队、几百个账号；授权主要靠插件完成，控制台主要用来"总体看数据 + 踢人 / 邀请 / 轮转"。

---

## 0. 结论先看

1. **最该做的是"长任务进度栏"**。现在踢人、邀请、补位、轮转一提交，按钮就变成"处理中"。进度只写在抽屉表单里的一行小字 `阶段 · N 秒`，抽屉一关就看不到了。任务抽屉里的"步骤"也只是一张键值表。建议做一个统一的**任务进度组件**，在四个地方出现：团队卡、团队抽屉、顶栏任务中心、任务详情。它显示第几步、当前步骤、用时、卡在哪、下一步该做什么。第 2 章展开。
2. **设置页的样式其实是坏的**。大约 10 个 class 在 CSS 里完全没有定义（`settings-columns`、`setting-row`、`settings-savebar`、`input-with-unit` 等），是 `301feb3` 那次改版丢的。这是纯缺陷，修复成本低、收益高，建议第一批就修。
3. **账号页太挤**：一行约 140px 高，同一个事实会在多列重复出现，"旧快照"琥珀色标记每一行都有，团队卡头部有 7 个可点元素。按"异常优先、一处一真相"重排，目标行高 56–64px。
4. **颜色语义冲突**：红色同时表示"去授权"（正向动作）、"删除团队"（不可逆）、"401"（异常），应该只留给不可逆操作和真正的故障。
5. **一致性债**：状态样式有三套，主按钮颜色两种，h1 有两种大小，"团队 / 工作区 / 工作空间 / Team"四种叫法，关闭按钮有"关掉 / 关闭 / 取消"三种写法，焦点环两套叠在一起。

**启发式评分（Nielsen 10 项）：18 / 40**。内容是为这个产品量身做的，信息架构却还是通用后台模板的样子。

| # | 启发式 | 分 | 主要扣分点 |
|---|---|---|---|
| 1 | 系统状态可见 | 2 | 长任务进度看不到；侧栏"控制台已连接"是写死的 |
| 2 | 贴近真实世界 | 2 | 中英混杂（Binding / Membership / Owner）；同一概念四种叫法 |
| 3 | 用户控制与自由 | 2 | 长任务提交后没有入口暂停或取消（后端其实有 `/cancel`） |
| 4 | 一致性与标准 | 1 | 三套状态徽章、两种主按钮、两种 h1、两套分页 |
| 5 | 错误预防 | 2 | 轮转要手打邮箱；"删除团队"常驻在卡头 |
| 6 | 识别优于回忆 | 2 | 接码号输入框要求记住线格式；关键解释藏在 `title` 悬停提示里 |
| 7 | 灵活与效率 | 2 | 命令面板缺"任务"；搜索没有防抖；菜单不能用方向键 |
| 8 | 极简美学 | 1 | 账号行信息过载；首屏被自动轮转说明占掉 |
| 9 | 错误识别与恢复 | 2 | 兜底文案 `请求失败: ${key}`；踢人成功只提示"成员已更新" |
| 10 | 帮助与文档 | 2 | 禁用按钮不说明原因 |

**检测器（B）结果**（对渲染后的 4 个页面扫描，共 20 条）：
- `undersized-ui-text`（警告，4 页都有）：侧栏分组标题"资源"是 10px。CSS 中另有 5 处 10px：`app.css:301`、`components.css:156/402/439/462/723`。
- `cramped-padding`（警告 4 条）：账号页 `.toolbar` 底边、`.management-table-scroll` 四边、总览某容器左侧、设置页 `.panel-header` 上下内容贴边。
- `gpt-thin-border-wide-shadow`（提示 12 条）：卡片用"1px 边框 + 32px 模糊阴影"，深色主题下显得发灰发脏。
- 此前直接扫模板得到的：`flat-type-hierarchy`（h1/h2/h3 都是 16px），em-dash 偏多（10 处，提示级）。

---

## 1. 设计原则（本次改动的判断标准）

来自 `PRODUCT.md`，后面每一条改动都要能对上其中至少一条：

1. **异常优先**：正常项让位，异常项和下一步动作永远最显眼。
2. **一处一真相**：同一事实（授权状态、Sub2API 状态、额度新旧）只显示一次。
3. **长任务可追踪**：任何后台 Operation 都能看到走到第几步、卡在哪、下一步是什么。
4. **危险操作说清后果**：改官方 Team 和只改本地，在确认框里明确区分。现有 `removeTeamMember` 的确认文案（`app.js` 3179–3209）就是好的范本，保留。
5. **保留视觉与测试契约**：保留已有选择器和 aria 名称；确实要改的，同步更新 `tests/browser_*.py` 和 `tests/*.test.cjs`。

---

## 2. P0：长任务进度栏（踢人 + 邀请 + 补位 + 轮转）

### 2.1 现状与根因

| 现象 | 位置 | 根因 |
|---|---|---|
| 邀请、补位时只有表单里一行 `stage_label · N 秒` | `app.js` 3074 `watchInviteProgress` | 每 2 秒轮询 `/api/runtime/status`，按 `workspace_id + kind` 找任务；结果只写进表单内的 `role=status` 段落 |
| 抽屉一关，进度就没了 | 同上 | 进度绑定在表单 DOM 上，没有全局状态 |
| 轮转、踢人完全没有进度，只有按钮上的"轮转中 / 处理中" | `app.js` 2797、3211 | 没有调用 `watchInviteProgress` |
| 结束后只弹一条 toast："成员已更新" / "轮转已提交" | `app.js` 2805、3222 | `handleActionResult` 用的是通用文案，没有拿结果里的席位和调度信息 |
| 任务详情"步骤"是 `step_name: 状态` 键值表，显示的是英文键名 | `app.js` 1746–1748 | 没用后端已经给出的 `step_label` |
| 入组任务打开详情时没有步骤 | `onboard.py` 用 `_progress` 调 `operation_store.note()`，只写 `log_json` 和 `current_step`，不写 `operation_steps` | 入组的阶段只存在于日志里 |

**关键约束（决定实现方式）**：`/api/workspaces/{id}/onboard`、`/replenish`、`/rotate`、`/kick` 在 `console_actions.py`（595–839）里都是在一个请求内同步跑完整个流程，**结束后才返回 `operation_id`**。所以前端在任务进行中拿不到 id，只能像现在这样按 `workspace_id` 反查 `/api/runtime/status`。

### 2.2 方案选择

| 方案 | 做法 | 优点 | 代价 |
|---|---|---|---|
| **A（推荐第一步）** 纯前端 | 做一个全局的 `taskTracker`，统一轮询 `/api/runtime/status`（已经有 2 秒 / 15 秒的自适应节奏），按 `workspace_id` 和 `kind` 把活动任务分发给各个显示位置 | 不动后端，风险最小 | 进行中拿不到步骤列表，只有当前阶段；N/M 靠前端预设的阶段表估算 |
| **B（第二步）** 后端小改 | ① 这 4 个接口先 `create` 任务、`commit`，立刻返回 `{operation_id, state:"running"}`（202），流程放进后台任务跑；② `runtime_operation()` 多返回 `stage_code`（已有）、`account_email`、`log_tail`（最近 3 条安全日志） | 前端可以直接用现有的 `startCurrentOperation` 和 `pollCurrentOperation`，刷新页面后还能恢复（sessionStorage 已经实现） | 要改接口的返回时机，需要回归 `tests/` 里入组、轮转相关的后端测试 |

建议先做 A，把 UI 定型；再做 B，把数据源换成按任务 id 精确轮询。两步之间 UI 组件不用变。

### 2.3 阶段表（前端显示 "第 N / M 步" 的依据）

在 `app/application/presenters.py` 里新增 `OPERATION_STAGE_PLANS`，由 `/api/runtime/status` 和 `/api/operations/{id}` 一并下发。这样前端不用硬编码，将来改流程也只改一处：

| 任务类型 | 阶段（code → 现有中文标签） | M |
|---|---|---|
| 邀请 / 入组 `onboard` | checking 检查成员与席位 → hme 领取邮箱（选填邮箱时跳过）→ inviting 发送官方邀请 → invite_mail 等待邀请邮件 → browser 注册与入组（子阶段：email_otp / about_you / add_phone / sms_otp / oauth）→ reconciling 确认官方入组 → authorizing 自动授权 → active 已完成入组 | 7（显示时跳过的阶段标"跳过"） |
| 补位 `replenish` | 同 onboard（补位内部就是走一次入组） | 7 |
| 受控轮转 `rotate` | preflight 核对补位资源和原席位 → confirm_trigger 确认轮转条件 → paused 暂停旧账号调度 → drained 等待旧请求结束 → official_removed 或 kicked 旧账号离组 → refill 完成补位与同步（内部嵌套入组子阶段） | 6 |
| 踢人 `kick_member` | 暂停 Sub2API 调度 → 移出官方团队 → kicked 复读确认（`rotate.py` 570 `kick_to_standby` 目前只写了 `kicked` 一步，前两步需要补 `mark_step`，实施时先核对） | 3 |

`browser` 的子阶段（email_otp、add_phone、sms_otp 等）不单独占一格，显示成当前格的副标题，例如"第 4/7 步 · 注册与入组 · 等待短信验证码"。

### 2.4 统一的进度组件 `task-progress`

一个组件，三种密度，四个出现位置：

**① 团队卡头部（紧凑，一行）** 位置：`accounts-view.js` 330–399 `groupNode`
```
[补位中] 第 4/7 步 · 注册与入组 · 等待短信验证码 · 2 分 10 秒   查看
▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░
```
- 有活动任务时，这一行替换掉卡头的"今日切换 / +1 / 到期日期"那一行（见 3.3），任务结束 10 秒后恢复原样。
- 失败或需要人工时，行变为警告色，写明原因和动作，例如"第 4/7 步失败 · 短信验证码超时 · 重试 / 查看"。
- 进度条是分段条，每段对应一个阶段；已完成段实心，当前段带斜纹动画（遵守 `prefers-reduced-motion`），失败段用警告色。不能只靠颜色，失败段上要叠一个图标。

**② 团队抽屉（标准，竖向步骤条）** 位置：`app.js` 2873–2990 `renderTeamDetails`，放在抽屉最顶部，已管理账号列表之前
```
正在补充 Team · 已用时 2 分 10 秒                    [取消任务] [查看详情]
✓ 检查成员与席位                               3 秒
✓ 领取邮箱  hme-xxxx@icloud.com                 1 秒
✓ 发送官方邀请                                 4 秒
● 注册与入组 · 等待短信验证码                  2 分 02 秒
○ 确认官方入组
○ 自动授权
○ 已完成入组
```
- 表单提交后自动收起，由步骤条接管，不再在表单里写一行小字。
- "取消任务"调用现有的 `/api/operations/{id}/cancel`（方案 B 之后才能拿到 id；方案 A 下用 runtime status 里的 `id` 字段，它已经是 public_id）。取消前走 `confirmDanger`，写明"已发出的官方邀请不会自动撤回"。
- 结束时步骤条保留，并在底部显示**结果摘要**（见 2.6），直到用户点"收起"或发起新任务。

**③ 顶栏任务中心（全局）** 位置：`console.html` 41–60 `.topbar-actions`
- 常驻一个按钮：没有任务时显示"任务"，有任务时显示"2 个进行中"，加一个小圆点；需要人工时圆点变成警告色，并显示"1 个需处理"。
- 点开是浮层（复用 `overlayRegistry`），列出活动任务，每条是紧凑版进度组件，再加最近 5 条已结束的任务（数据已在 `recent_operations` 里）。
- 这样在任何页面、关掉抽屉以后，都能看到踢人 / 邀请走到哪一步。它替代侧栏底部写死的"控制台已连接"（`console.html:36`），真实状态改从 `runner.state` 读。
- 数据源：把 `runtime-status.js` 里的 poller 提升为全局单例 `window.Team48Runtime`（现在只在总览页加载，`console.html:709`），其他页面订阅它，避免每个组件各自轮询。

**④ 任务详情抽屉（完整）** 位置：`app.js` 1718–1751
- "步骤"区从键值表换成与 ② 相同的竖向步骤条；标签用 `step_label`，不要用 `step_name`。
- 入组类任务没有 `steps[]` 时，用 `log[]` 里的 `stage` 字段加上阶段表拼出步骤条（兼容历史任务）。
- 失败步骤要写：原因（`error_message` 经 `friendlyError` 翻译）、需要人做什么、按钮（重试 / 去授权 / 查看账号）。
- 日志默认折叠在步骤条下方，显示最近 8 条；"技术详情"里放原始 `error_code` 和 `result`。

### 2.5 账号行上的进度

位置：`accounts-view.js` 211–285 `accountRow`
- 账号正在被入组、轮转、踢出时，"授权与检测"列换成一个小徽章，如"入组中 · 4/7"，点击打开任务详情。
- **手机验证失败 / 卡住的账号**，现在看起来和从没开始过的待命账号一模一样。改为显示"入组中断 · 手机验证"，并给出"继续 / 放弃"两个动作（后端已有续跑逻辑：`invitation_flow.prepare` 会优先续跑未完成的子号）。

### 2.6 结束后的结果摘要（替代"成员已更新"）

| 任务 | 成功时 | 部分成功 / 需人工时 |
|---|---|---|
| 踢人 | "已移出 a@x.com · 席位 5/6 · Sub2API 调度已暂停（未解绑）" | "已移出官方团队，但 Sub2API 暂停失败：xxx · 去 Sub2API 处理" |
| 邀请 / 补位 | "b@x.com 已入组并授权 · 席位 6/6 · 已推送 Sub2API" | "已入组但授权未完成 · 在插件里完成授权 / 打开授权" |
| 轮转 | "a@x.com → b@x.com 轮转完成 · 今日第 2 次（上限 2）" | "旧号已移出，补位未完成：vacancy 闸拦住补位 · 查看" |

数据来自各流程的 `result_json`，前端写一个 `summarizeOperationResult(kind, result)`，放在 `handleActionResult` 和 `finishCurrentOperation` 里共用。

### 2.7 顺带修的流程问题

- **轮转表单要手打邮箱**（`app.js` 2766–2814）：改成下拉选择团队里的在席成员，每项显示邮箱、5h/7d 额度和授权状态，并按"最该轮"的顺序排：401 优先，其次 7d 用尽。补位邮箱保持可选，写明"留空则从待命池选"。
- **轮转按钮是 danger 红**：改成主按钮样式，靠确认框说清后果；红色留给不可逆操作。
- **接码号输入框的 placeholder 是完整线格式**（`app.js` 2687）：placeholder 改成"+1 号码----接码链接"，下方加一行格式说明；如果号池里有可用号，默认从号池分配，手填放进"更多参数"。
- **补充 Team 的表单写死"官方角色：Owner"**（`app.js` 2681），和邀请表单的角色下拉不一致。统一为同一个角色控件，补位的默认值取原席位角色。
- **"强制跳过部分门禁"和"跳过官方邀请"复选框创建了但没加进表单**（`app.js` 2597–2609，`moreBody` 只 append 了 phone 和 proxy）。要么删掉，要么放进"更多邀请参数"并写清后果。提交时却还在读 `force.checked`，这是潜在 bug，需要确认本意。

### 2.8 验证

- 单测：新增 `tests/task_progress_ui.test.cjs`，覆盖阶段表映射、N/M 计算、失败段渲染、`summarizeOperationResult`。沿用 `invite_seat_ui.test.cjs` 的 jsdom 写法。
- 浏览器：扩展 `tests/browser_runtime_lifecycle.py` 和 `tests/browser_automatic_rotation.py`，用预览数据造一个 running 的 onboard 任务，断言团队卡进度行、顶栏计数、抽屉步骤条、关闭抽屉后顶栏仍然可见。
- 方案 B 另需：后端接口改为立即返回的回归测试（入组、补位、轮转、踢人各一条），以及刷新页面后恢复轮询的测试。

---

## 3. P0 / P1：账号与团队页（额度巡查与轮转决策）

### 3.1 首屏

| 问题 | 改动 | 文件 |
|---|---|---|
| "自动轮转"说明面板占了首屏大块（截图 1440 下约 190px） | 折叠成一行状态条："自动轮转 已关闭 · 范围 0 个团队 · 每日上限 2 · 设置"。展开后才显示说明和范围选择 | `console.html` 124–140；`auto-rotation.js` |
| 4 张 KPI 卡点不了 | 卡片改成筛选入口：点"需授权 7"就切到待处理视图并带上筛选条件 | `console.html` 140–160；`accounts-view.js` |
| Sub2API 没配置时，整列都在显示"未配置" | 隐藏 Sub2API 列，改为顶部一条横幅"Sub2API 未配置，计费与同步不可用 · 去设置" | `accounts-view.js` 286–321 |
| "核对 Sub2API 状态"按钮是禁用的，但不说明原因 | 按钮旁边直接写原因（"未配置"/"正在核对"），不要只放在 `title` 里 | 同上 |
| 筛选条在 1440 宽下换成两行 | 常用的（搜索、用途、授权状态）留在一行，其余收进"更多筛选"，并显示已启用的筛选数量 | `console.html` 170–200 |

### 3.2 账号行（目标 56–64px 高）

位置：`accounts-view.js` 211–285 `accountRow`、286–321 `table`

| 列 | 现在 | 改后 |
|---|---|---|
| 账号 / 本地用途 | 邮箱 + 用途 + "Business · 档位未识别"三行 | 邮箱一行；用途用小标签接在邮箱后面；官方计划只在异常时显示（"档位未识别"改为悬停提示 + 详情） |
| 授权与检测 | "待完成 OAuth · 缺少刷新凭据"红色块，会换行 | 一个状态徽章，文字不超过 6 个字（"需 OAuth"、"401"、"正常"），详细原因放进详情。有进行中任务时显示进度徽章（2.5） |
| 官方额度 | 5h / 7d 两根条 + "旧快照 · 3 天前" | 两根条加百分比数字；**加 7d 重置时间**（数据已有：`seven_day_reset_at`，见 `domain/quota.py:48`），如"7d 92% · 周四重置"；"旧快照"只在超过阈值（例如 6 小时）时显示，并且整行只显示一次 |
| 远端 #id / 近 7 天成本 | 常驻 | 移进悬停提示和详情 |
| 操作 | "授权"（红色）+"…" | "授权"改成主按钮样式；1024 宽时操作列固定在右侧（`position: sticky; right: 0`），不再被挤出视口 |

- `min-width: 1100` 放宽到 960，配合上面的精简。
- **"…"菜单子号最多 12 项**：按"常用 / 同步 / 危险"三组分隔，危险组放最后并用分隔线隔开；隐藏当前状态下不可用的项。另外修一个 bug：`accounts-view.js:560` 检查的是 `action.when`，注册表里的字段叫 `visible`，所以现在隐藏逻辑其实没生效。
- 排序：按官方额度排序时团队会被拆到不同页。建议"按团队"视图下只在团队内部排序，团队本身按"最差成员的额度"排序。

### 3.3 团队卡头部（7 个可点元素减到 2 个）

位置：`accounts-view.js` 330–399 `groupNode`

现在：展开箭头、团队名、"3 个需授权"、"填写到期日期"、"今日切换 0 次"、"+1"、"删除团队"（danger，常驻）、"管理团队"。

改后：
```
▸ North · 研究团队  [3 需授权]  3/6 席 · 到期 12 天 · 今日轮转 0/2       [管理团队] [⋯]
  （有任务时，这里多一行进度，见 2.4 ①）
```
- "今日切换"改由系统在轮转成功时自动计数，只做展示，去掉手动"+1"按钮。如果确实需要手动修正，放进"⋯"菜单的"校正今日轮转次数"。需确认 `workspace_switch_count` 的写入路径。
- "填写到期日期"：没填时显示成淡色的"到期未填"，点击打开抽屉里的到期区块。
- "删除团队"移进"⋯"菜单，改名"移除本地记录"，确认框写明"不会影响官方 Team"。这也和抽屉里把它放在"危险操作"折叠区的做法统一了。
- 团队代理"直连"改为只在异常时显示（例如代理失效）。

### 3.4 团队抽屉（管理团队）

位置：`app.js` 2873–2990 `renderTeamDetails`

截图问题：抽屉顶部先是"已管理账号"三个大输入框样式的块，然后是团队概览、到期日期大卡片，要滚两屏才看到成员和邀请。邀请和补充两个表单并排挤在一起，一个表单用两列，窄得看不清。

改后的顺序：
1. **头部**：团队名 + 状态标签 + 席位 3/6，右上角"关闭"（统一文案）。
2. **进度步骤条**（仅在有任务时出现，2.4 ②）。
3. **成员列表**（主体）：母号置顶，子号按"需处理 → 在席 → 已邀请 → 已离队"排序。每行：邮箱、角色、状态、额度迷你条、"⋯"。"已管理账号"的三个块并进来，不再单独占一段。
4. **主动作条**：`[邀请成员] [补充 Team] [受控轮转]` 三个按钮一排，点击后在下方展开**单个**表单（一次只展开一个），单列布局。
5. **团队信息**（折叠）：Workspace ID、最近同步、到期日期（从大卡片缩成一行："到期 2026-10-08 · 还剩 12 天 · 修改"）、团队代理。
6. **危险操作**（折叠，放最后）。

- 母号的"重新授权"是红色按钮：改为主按钮样式。
- "Workspace ID"和团队名下的长 UUID 重复出现：只在"团队信息"里显示一次，旁边加复制按钮。

### 3.5 待处理视图与总览的数字对齐

- 总览"待处理 10"和账号页"需要授权 7"对不上：统一一个 `needs_attention` 口径（授权问题 + 入组中断 + 额度用尽 + 同步失败），两个页面都用同一个后端字段，并在数字旁边写明构成，例如"10（授权 7 · 入组中断 2 · 同步失败 1）"。

---

## 4. P1：总览页

位置：`console.html` 65–100；`app.js` 1580–1660；`runtime-status.js`

| 问题 | 改动 |
|---|---|
| 待处理列表每条邮箱出现两次（`app.js:1599` 调用 `twoLine(item.email \|\| item.message, item.message)`） | 第一行邮箱，第二行原因；没有邮箱时第一行显示团队名 |
| 同一账号可能出现两条 | 按账号合并，原因合并成一行（"401 · 额度用尽"） |
| 两栏宽度失衡：待处理挤在窄栏，资产栏下面大片留白 | 待处理放到宽栏在前；工作区资产变成紧凑表格放右侧，或放到下方 |
| KPI 卡不能点 | 点击跳到对应筛选（同 3.1） |
| 副标题在描述布局（"工作区资产在左，待处理在右"） | 改成说明用途，例如"先处理异常，再看资产" |
| runtime 区块只在总览存在 | 保留在总览作为"完整版"，同时它的 poller 提升为全局，供顶栏任务中心用（2.4 ③） |
| "15 秒 / 30 秒"文案不一致：远端核对轮询实际是 15 秒，文案写的是 30 秒 | 文案按代码改成 15 秒，或者抽成一个常量 |

---

## 5. P0：设置页修复 + P1：设置与 Sub2API 同步排障

### 5.1 补上丢失的 CSS（P0，纯缺陷）

以下 class 在模板和 JS 里用到了，但 `app/web/static/css/*.css` 里一处定义都没有：
`settings-columns`、`setting-row`、`settings-savebar`、`service-card`、`input-with-unit`、`password-fields`、`settings-probe-actions`、`is-dirty`、`text-ok`、`page-feedback`。

用 `git show 301feb3^:app/web/static/css/...` 找回旧定义作为起点，再按下面的布局补齐：
- `settings-columns`：1440 下两列（左边服务连接，右边自动化与后台账号），1024 以下一列。
- `setting-row`：CSS 网格，标签列 200px，控件列最宽 320px，说明文字放在控件下方，12px。
- `input-with-unit`：单位做成输入框内的后缀（"秒"、"次"、"%"），不要换行。
- `service-card`：每个外部服务一张卡：标题 + 连接状态徽章 + "测试连接"按钮 + 最近一次测试结果与时间。
- `settings-savebar`：吸底保存栏，显示"N 项未保存 · 放弃修改 · 保存设置"。没有改动时隐藏。`is-dirty` 标记改过的字段（左侧加一条色条）。
- fieldset 去掉浏览器默认的边框和 legend 样式，改成与面板一致的分组标题。
- 复选框改成开关样式，标签贴在开关旁边（现在两者之间隔得很远）。

### 5.2 信息架构

位置：`console.html` 356–555；`app.js` 3700–3900

建议分区（左侧锚点导航，或者顶部分段按钮）：
1. **外部服务**：Sub2API、iCloud HME、临时邮箱、手机号池。每个一张 `service-card`。
2. **自动化**：自动额度检查、令牌刷新、自动重新授权、自动轮转（含范围选择，`app.js` 3709–3745）。每项一行：开关 + 频率 + "下次运行时间"（数据已在 `runtime_status.policies[].next_run_at` 里）。
3. **后台账号**：单独一区，放最后（现在"保存设置"按钮夹在它上面，容易误解）。

### 5.3 Sub2API 排障路径

目标：同步失败时，从看到异常到定位原因，最多两步。
- `service-card` 上直接显示最近一次同步结果："同步用量 3 分钟前成功 / 推送失败 2 条"。点击后列出失败的账号和原因，每条带"重试"。
- "测试连接"的结果分层显示：网络是否可达 → 鉴权是否通过 → 分组 / 代理目录能否读取。哪一层失败就在哪一层写原因和建议，不要只报"请求失败: sub2api-probe"。
- 兜底文案 `请求失败: ${key}`（`app.js:282`）改为"操作没有完成（{中文动作名}）· 技术详情"，并把常见的 `error_code` 补进 `ERROR_CODE_MESSAGES`（`app.js` 135–157）：`remote_catalog_unavailable`、`proxy_unresolvable`、`operation_conflict`、`vacancy_not_safe_to_refill` 等。
- 账号行里的 Sub2API 状态和设置页的连接状态共用同一个来源（一处一真相）。设置里没配置时，账号页就隐藏这一列（3.1）。

### 5.4 验证

- `tests/browser_sub2api_defaults.py`、`tests/browser_sub2api_state.py`：保存、放弃、dirty 计数、测试连接的分层结果。
- 新增：设置页 1440 和 1024 截图对比，断言单位和输入框在同一行、保存栏吸底。

---

## 6. P1：任务记录页

位置：`console.html` 211–281；`app.js` 4400–4500

- 列表每行加一个迷你进度（紧凑版组件），进行中的任务置顶。
- 默认筛选"进行中 + 需人工"，已结束的放在第二个标签里。
- 分页统一：账号页用 10/20/50/100（默认 20），任务页用 25/50/100（默认 50）。统一成 20/50/100，默认 50。
- 导航里叫"任务记录"，`pages.py` 的 `NAV` 里叫"任务"，命令面板里没有这一项：统一叫"任务"，命令面板补上"任务"、"进行中的任务"。
- 需人工的任务在行内直接给动作（重试 / 取消 / 打开账号），不用非得进详情。

---

## 7. P1：颜色语义

| 语义 | 用途 | 现状问题 |
|---|---|---|
| 主色（蓝） | 主按钮、当前选中、进度的已完成段 | 账号页主按钮是蓝色，其他页是深色 |
| 危险（红） | **只用于**不可逆操作按钮（永久删除、移出官方团队）和确认框里的危险按钮 | 现在"授权"、"重新授权"、"执行受控轮转"、"删除团队"都用 danger |
| 故障（红色系徽章） | 401、同步失败、任务失败 | 和"待完成 OAuth"用同一种红，看不出轻重 |
| 警告（琥珀） | 需要人处理但不紧急：待 OAuth、快照过旧、需人工 | "旧快照"每一行都有，把警告色用成了背景噪音 |
| 中性 | 正常、已完成、未配置 | — |

- 三套状态样式（`.status[data-tone]`、`.badge-*`、`.management-badge.tone-*`）合并成一套 `.status[data-tone]`；另两套先做别名，保证测试选择器不断，再逐步迁移。
- 状态不能只靠颜色：每个徽章都有文字，失败类的再加一个图标。
- 卡片的"1px 边框 + 32px 模糊阴影"（检测器提示 12 条）：深色主题下去掉大阴影，只留边框和 1–2 级面板底色差；浅色主题保留小阴影（最大 8px）。

---

## 8. P2：字号与排版

位置：`tokens.css`、`app.css`、`components.css`、`management.css`

- 正文基准 13px 改为 **14px**；**最小 12px**。现在有 25 处 ≤ 11px，其中 6 处是 10px（见检测器结果）。
- 标题层级：h1 20px（全站统一；现在账号页 24px、其他页 18px），h2 16px 半粗，h3 14px 半粗加字距。解决 `flat-type-hierarchy`（h1/h2/h3 都是 16px）。
- 数字（额度、席位、金额、用时）用 `font-variant-numeric: tabular-nums`，避免进度计时时宽度跳动。
- `management.css` 是压缩成一行一行的写法，约 123 处写死的 px：先格式化（只调格式，不改规则），再把间距和字号替换成 `tokens.css` 里的变量。
- 断点从 12 个收敛到 3–4 个（1280 / 1024 / 720），去掉几乎重复的 1279/1280 和 719/720。
- `cramped-padding` 的 4 处：`.toolbar` 底部、`.management-table-scroll`、总览左侧容器、设置 `.panel-header` 各补 8–12px 内边距。

---

## 9. P1：一致性与文案（适合用 `/impeccable clarify`）

| 项 | 统一为 |
|---|---|
| 团队 / 工作区 / 工作空间 / Team / Workspace | 界面统一叫"**团队**"；只在"Workspace ID"这种技术字段里保留英文 |
| Owner / Member | "**母号角色（Owner）/ 成员（Member）**"首次出现时中英对照，之后只用中文 |
| Binding / Membership 等英文残留 | 全部换成中文 |
| 关掉 / 关闭 / 取消 | 浮层右上角统一"**关闭**"；表单里的撤销统一"**取消**" |
| "授权失效" / "401" / "需要授权" | 徽章用短词（"401"、"需 OAuth"、"缺凭据"），详情里用完整句 |
| 按钮文案 | 动词开头、说清对象："发送邀请"、"开始补位"、"执行轮转"、"移出团队" |
| em-dash（检测器提示 10 处） | 中文界面改用"·"或逗号 |

- 很多关键解释只放在 `title` 属性里（悬停才看得到，键盘和读屏用户看不到）。重要的改成可见的辅助文字，次要的改成可聚焦的提示按钮。
- `pages.py` 里没用的 `NAV` 元组删掉，或者让模板真的用它渲染侧栏（二选一，避免两处各写一份）。

---

## 10. P1：无障碍与键盘

- **焦点环**：现在有两套，叠成双层。统一为一套 `:focus-visible { outline: 2px solid var(--focus); outline-offset: 2px }`，删掉组件里各自的 box-shadow 焦点样式。
- **菜单**：`role=menu` 的"⋯"菜单和登记菜单支持方向键、Home/End、Esc，焦点回到触发按钮。登记菜单现在贴着视口右边缘，要做边缘翻转。
- **表单标签**：搜索框和筛选下拉补上 `aria-label` 或可见标签。
- **`aria-live`**：现在整张表重新渲染时会让读屏器把整张表重读一遍。改成只对一个专门的状态区域（"已加载 20 条 / 筛选后 7 条"）使用 `aria-live=polite`；进度组件的阶段变化也只在阶段切换时播报，不播报每秒的计时。
- 搜索框加 200ms 防抖（`accounts-view.js:609`）。
- 命令面板（Ctrl+K）："去团队"还指向已废弃的 `/workspaces`，改成 `/accounts?view=teams`；补上"任务"和"进行中的任务"。

---

## 11. P2：清理

- `console.html` 101–123：`/workspaces` 页的死代码块。
- `app.js` 4085–4277：死代码（调研时确认没有调用方，删之前再用 Grep 复核一次）。
- 没用到的 class：`skeleton-row` 等。
- 侧栏"控制台已连接"改为真实状态，或者删掉（由顶栏任务中心替代）。
- `watchInviteProgress` 被 `taskTracker` 取代后删除。

---

## 12. 不改的内容

- 整体配色、深色默认主题、侧栏加顶栏的骨架、Logo、中文语气。
- 技术栈：不引入框架、UI 库、构建链；不加 web font（`PRODUCT.md` 里记为未决）。
- 手机端：只保证"能看、不塌"，不做专门布局（390 截图里顶栏按钮换行、表格被截断，只修到不溢出为止）。
- 危险操作确认框的文案结构（`removeTeamMember` 那套写得很好，推广到其他地方）。
- 授权流程本身：授权闭环主要在插件里完成，控制台的授权抽屉只做文案和按钮颜色调整。
- 后端业务逻辑：除 2.2 方案 B 的"接口立即返回"和 2.3 的阶段表下发以外，不改。

---

## 13. 实施顺序

每一批都能单独上线，单独回归。

| 批次 | 内容 | 章节 | 风险 | 建议用的 impeccable 命令 | 回归 |
|---|---|---|---|---|---|
| **1** | 设置页补 CSS；红色语义纠正（授权 / 轮转改主按钮）；总览邮箱重复；`action.when` → `visible` bug；兜底错误文案 | 5.1、7、4、3.2、5.3 | 低 | `layout settings`、`harden` | `browser_sub2api_*`、`browser_ui_polish.py`、`management_ui.test.cjs` |
| **2** | 进度组件（方案 A）：阶段表、团队卡进度行、抽屉步骤条、顶栏任务中心、任务详情步骤条、结果摘要 | 2 | 中 | `shape task-progress` → 实现 → `polish` | 新增 `task_progress_ui.test.cjs`；`browser_runtime_lifecycle.py`、`operation_notifications.test.cjs` |
| **3** | 账号页信息架构：首屏折叠、行精简、团队卡头部、抽屉重排、轮转成员下拉 | 3、2.7 | 中高（选择器变动最多） | `distill accounts`、`layout` | `browser_management.py`、`browser_workspace_*.py`、`workspace_*_ui.test.cjs`、`invite_seat_ui.test.cjs` |
| **4** | 后端方案 B：接口立即返回 `operation_id`，前端改为按 id 轮询 | 2.2 | 中（接口行为变化） | 不需要 | 后端入组 / 轮转测试；`browser_automatic_rotation.py` |
| **5** | 字号、标题层级、断点收敛、`management.css` 格式化 | 8 | 中（视觉面广） | `typeset`、`adapt` | 全部 `browser_*.py` 截图回归；`browser_zoom_check.py`、`browser_theme.py` |
| **6** | 文案统一、无障碍、键盘、清理 | 9、10、11 | 低 | `clarify`、`audit`、`harden` | `browser_ui_polish.py`；最后跑一次全量回归 |

最后用 `/impeccable critique` 复评，目标从 18/40 提到 30/40 以上，重点看 #1（系统状态可见）、#4（一致性）、#8（极简）。

---

## 14. 风险与注意

- **测试契约**：`tests/browser_*.py` 大量依赖 `.management-table`、`button name="管理团队"`、`"邀请成员"`、`"补充 Team"`、`"更多操作"` 等选择器和可访问名称。批次 3 前先 Grep 出所有被引用的选择器，列成清单；改名时同步改测试，一次提交。
- **方案 B 的并发**：接口改成立即返回后，要保证 `create_workspace_locked` 的互斥仍然有效（同一团队同时只能有一个变更任务）。前端收到 `operation_conflict` 时，应直接跳到那个正在跑的任务的进度，而不是只报错。
- **轮询压力**：全局只保留一个 `/api/runtime/status` poller（有任务时 2 秒，没有时 15 秒），各组件订阅它，禁止每个组件各自 `setTimeout`。
- **预览数据有限**：本次评审基于 `tests/preview_app.py` 的示例数据（3 个团队、7 个账号），隔离预览里写操作一律返回"隔离预览不执行账号操作"。没有看到真实的进行中任务、真实的 Sub2API 失败、几百行数据时的性能。进度组件和大数据量表现要在批次 2、3 完成后，用真实数据或造的数据再确认一次。
- **生产环境**：所有改动只在本地开发和预览（8019）上验证；部署按 `AGENTS.md` 只动 `/opt/team48` 和 `team48` 这个 Compose 项目，不碰 `/opt/sub2api`。

---

## 附录 A：用户旅程（踢人 + 邀请），改前 vs 改后

**改前**
1. 账号页找到团队 → 点"管理团队" → 抽屉里往下滚两屏找到成员
2. 成员行"⋯" → "移出官方团队" → 确认 → 按钮"处理中"……几十秒后 toast"成员已更新"
3. 点"邀请成员" → 表单 → "发送邀请" → 表单里一行小字"注册与入组 · 87 秒"
4. 想去别处看看 → 关掉抽屉 → 进度消失 → 只能去任务记录页，在列表里找
5. 失败了 → toast 里点"技术详情" → 抽屉里是键值表：`browser: failed · ...`

**改后**
1. 账号页团队卡头部就显示席位 3/6 和"3 需授权" → 点"管理团队" → 抽屉顶部就是成员列表
2. 成员行"⋯" → "移出官方团队" → 确认 → 抽屉顶部出现步骤条（3 步）→ 结束后摘要"已移出 · 席位 2/6 · Sub2API 调度已暂停"
3. 点"邀请成员" → 表单 → 提交后表单收起，步骤条接管："第 4/7 步 · 注册与入组 · 等待短信验证码 · 1 分 27 秒"
4. 关掉抽屉 → 团队卡头部有进度行，顶栏"1 个进行中" → 去别的页面照样能看到
5. 失败了 → 团队卡和顶栏变成警告色"第 4/7 步失败 · 短信验证码超时 · 重试" → 点进去是步骤条，失败步骤写着原因和按钮

## 附录 B：本次评审产物

- `PRODUCT.md`：产品上下文（impeccable init 产物，后续 impeccable 命令都会读取它）。
- `UI改动大纲.md`：本文件。
- 截图和检测器原始输出都是临时文件，已清理，没有提交进仓库。
